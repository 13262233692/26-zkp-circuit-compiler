[package]
name = "zkp-circuit-compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
num-bigint = { version = "0.4", features = ["rand"] }
num-traits = "0.2"
thiserror = "1"
hex = "0.4"
rand = "0.8"
